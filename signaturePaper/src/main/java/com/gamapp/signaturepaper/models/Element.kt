package com.gamapp.signaturepaper.models

import androidx.compose.ui.geometry.Offset

/**
 * an element to store offset and strokeWeight of a particular [Point]
 * */
class Element(val offset: Offset, val strokeWeight: Float)